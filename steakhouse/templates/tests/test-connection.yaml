apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "steakhouse.fullname" . }}-test-connection"
  labels:
    {{- include "steakhouse.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "steakhouse.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
